import { Interface } from "readline";

// function expandedKeys<T>(inputType: T) {
//     let obj = new inputType();
//     let values = Object.entries(obj);
//     values.map((key, value) => {
//         if (isPrimitive(value)) {
//             return key
//         } else {
//             return key + "." + test(typeof value);
//         }
//     })
// }

function isPrimitive(input: any) {
    return (test !== Object(test));
}